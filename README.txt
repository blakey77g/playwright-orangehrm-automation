OrangeHRM Playwright Automation

This project contains an automated Playwright test solution for the Recruitment > Candidates section of the OrangeHRM platform.

The automation demonstrates the use of Playwright, Page Object Model (POM), reusable page methods, stable locator strategies, assertions, and environment-based test credentials.

Security: Login credentials are not included in the repository. They are stored locally using environment variables and excluded from source control.